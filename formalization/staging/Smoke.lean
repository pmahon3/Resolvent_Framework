import Mathlib.Data.Real.Basic
import Mathlib.Order.Basic

example (a b : ℕ) : a + b = b + a := by sorry
example (s t : Set ℕ) (h : s ⊆ t) (x : ℕ) (hx : x ∈ s) : x ∈ t := by sorry
example (x : ℝ) (h : 0 < x) : x ≠ 0 := by sorry
example (n : ℕ) : n ≤ n + 5 := by sorry

# HANDOFF — the open `/audit full` on L_MO₂ concreteness (launch ticket)

> **TRANSIENT FILE. DELETE THIS once the audit runs and its verdict lands in
> `descent_axis_residue_post_kill.md`.** It is a launch ticket, not a doc — its
> only job is to make the audit runnable cold and to carry the discipline guard
> so a fresh session doesn't fumble the invocation or pre-place the result.

## The one question (do not re-improvise — copy from canonical)

**Is L_MO₂ concrete?** I.e. does it carry an order-determining (separating,
ideally 2-valued) family of states? Everything downstream collapses to this:
- **YES** → the "richness starves concreteness" conjecture is FALSIFIED **and**
  L_MO₂ is a candidate descent-axis contribution — the first live standalone
  lead since 2026-05-18 (serves curiosity (b), the incompatibility theorist).
- **NO** → L_MO₂ is another L(H)-analogue, conjecture stands, no contribution →
  park to `covered_leads/`.

(★) is already RESOLVED YES — L_MO₂ has genuine non-distributive descent
*structure*. That is established and is NOT what the audit re-checks. The audit
checks only **concreteness**, which (★) does not give.

## Where the substance lives (the ticket does NOT duplicate it)

`notes/open_questions/descent_axis_residue_post_kill.md` is the single source of
truth and is fully self-contained. Read it first. In particular:
- **Audit target + kill-risks, priority order** — its "THE NEXT MOVE" section
  (PRIMARY = concreteness / the horizontal-sum crux; SECONDARY = novelty vs
  localic/Navara; TERTIARY = already-published? Pták–Pulmannová, Gudder, Navara,
  Cannon–Döring).
- **The crux, stated** — V is a *horizontal sum (pasting)* of T×MO₂ copies, and
  pasting is the Greechie state-destroying mechanism. "Sub-OMP of concrete is
  concrete" covers the product and constancy-cut steps but **NOT** the
  horizontal sum. So concreteness is not a free corollary of the MO₂-swap.
- **SOURCES / ARTIFACTS** — Navara 1992 p. 428 (PDF in
  `literature_review/literature/navara_1992.pdf`), the Lean witness suite,
  the verification worksheets, the relevant memories.

## How to invoke (mechanics — easy to get wrong)

`/audit` has `disable-model-invocation: true` ⟹ it CANNOT be called via the
Skill tool. Launch an **opus Agent** with the audit instructions from
`.claude/skills/audit/SKILL.md` as the prompt (it gets WebSearch/WebFetch for
the literature checks). Include in the prompt:
- mode: **full**
- target/seed: `notes/open_questions/descent_axis_residue_post_kill.md`
- the contribution framework: `notes/programme/contribution_evaluation.md`
- the narrow instruction: **evaluate concreteness of L_MO₂ first (the
  horizontal-sum step), then novelty-vs-localic only if concrete.**

See `[[feedback_audit_invocation]]`.

## The discipline guard (the load-bearing warning)

**Do NOT fold anything into Paper II, do NOT call it a win, until the audit
resolves concreteness.** The asymmetry to guard against: the state-level
separation got a skeptical referee and DIED (2026-06-10, 5th decorative-σ
death); this lattice result must get the *same* referee. It must NOT ride the
green Lean checkmark — the Lean *axiomatizes* Navara's construction, and while
axiom-faithfulness is now hand-verified against p. 428, the *concreteness* and
*contribution* questions are entirely untouched by Lean.

## After the audit

1. Record the verdict in `descent_axis_residue_post_kill.md` (the routing
   section already enumerates both branches).
2. Update `program_overview.md` item 4's live-frontier paragraph (the ONE place
   that now carries detailed descent status after the consolidation — by design,
   so this is a single re-touch, not six).
3. Update the descent memories ([[oml_descent_inhabitation]] et al.) — one line.
4. **Delete this file** and remove any MEMORY.md pointer to it.

# Campaign 7 hostile audit

*2026-07-13. Verdict: mathematical chain survives at stated scopes; global
completion is not authorized.*

## Positive audit

The hand-proved chain `Phi iff GSD iff ODBC iff coarse ODBC` survives.
Fine CSR, countable-atlas T4At, relative closed eligibility, and coarse CIR
also survive with their recorded hypotheses. Boundary surgery depends on the
banked maximal-block/blockwise sigma-additivity theorem. **Evidence class:
hand proved.**

Lean scope is narrower: `ODBCSections.lean` fixes the outer finitely
additive state and proves an abstract sufficient packaging. It does not
formalize the note-level `X_p(J)` spaces, where the common `mu_J` may vary
existentially with `J`, nor the concrete ODBC/GSD equivalence. Meets and
countably generated blocks remain explicit dependencies of the Lean
`Phi -> T4At` theorem. **Evidence class: hand proved** as a statement audit.

## Negative audit

No abstract or Boolean obstruction is an admissible OML counterexample.
The Cantor eligible spaces are noncompact; Borel relation realization is
objectwise; common/deterministic transport does not pass noncentral
realization; the 44/56-event OMLs are finite Phi-tame controls; and the
two-coordinate inflation is unconstructed and has passed no gates. No fixed
finite pattern with every required state gate exists. **Evidence class:
hand proved** as a construction audit.

BOC remains an underdefined open programme, especially for coarse
nonstandard phenomena and the effective branch criterion. **Evidence class:
open.**

## Formal and repository audit

The two Lean files compile without `sorry`, `admit`, or project axioms.
Axiom receipts reproduce:

- countable-to-finite: `[Quot.sound]`;
- conditional compactness wrapper: none;
- `phi_of_odbc_sections` and both regime packages:
  `[propext, Classical.choice, Quot.sound]`.

**Evidence class: Lean verified.**

JSON parsing and `git diff --check` pass. Campaign commits are coherent.
The linked chain contains 44 meaningful iterations before this audit, below
the required 60. **Evidence class: executable verified.**

## Verdict

No legitimate stopping condition is met. Campaign 7 is complete, but the
chain must continue for at least 16 further meaningful iterations. The next
campaign attacks BOC normalization and the unconstructed two-coordinate
44-event inflation.

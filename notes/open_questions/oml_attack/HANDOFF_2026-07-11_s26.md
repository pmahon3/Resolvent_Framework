# OML lattice attack — session 26 handoff

**SUPERSEDED by `HANDOFF_2026-07-11_s27.md`; candidate killed at check 4.**

## Minimal read

Read this file and attack-note §20. Consult §§18–19 only for pentagon
notation. The candidate is now concrete enough that structural checks,
not further gadget invention, have priority.

## Candidate architecture

Use pentagon cells $B_i^n=\{x_i^n,y_i^n,x_{i+1}^n\}$ and glue

\[
x_2^n=x_0^{n+1},\qquad y_4^n=y_1^{n+1}.
\]

The implication

\[
(x_0^n,y_1^n)=(1,0)\Rightarrow(x_2^n,y_4^n)=(1,0)
\]

propagates uniquely. Adjacent cells close only five-cycles. Choose
$D_k=y_2^{3k}$ and adjoin one countably atomic master block with atoms
$D_k$. Consecutive master targets are at relay block-distance five, so
master cycles have length six.

The root cluster
$\{x_0^0,(y_1^0)^\perp,y_3^0\}$ forces the relay pattern and all
$D_k=0$. A free ultrafilter on the master block is the intended f.a.
cluster state; every σ-state on that block must charge some $D_k$.

## Priority — certify or kill four checks

1. Prove the infinite high-girth paste is an orthomodular lattice, or
   exhibit a missing meet/join.
2. Prove σ-completeness, including cross-block orthogonal sequences, or
   exhibit a missing countable join.
3. Prove the free master ultrafilter and relay assignments extend to a
   global f.a. two-valued state.
4. Prove σ-additive two-valued states are order-determining after the
   cluster state is excluded, yielding a concrete σ-class
   representation; or exhibit an unseparated order pair.

Run checks 1–2 together and 3–4 together. Do not call this a witness
until all four pass. Finite girth and relay propagation are already
verified.

## Key risks

No-four-loop Greechie folklore must be checked for this infinite diagram
and infinite master block; finite results may not imply σ-completeness.
The canonical representation on all f.a. states is inadmissible if it
fails to preserve countable joins. The correct point set is the
σ-additive states, which must still order-determine the abstract logic.

## Rolling cycle

Use two low-context agents split as above. Keep canonical edits
centralized. Propagate verified deltas through attack note, taxonomy,
shovel/live pointers, and the next handoff; validate JSON and diff. The
continuing goal remains active.

# OML lattice attack — session 27 handoff

**SUPERSEDED by `HANDOFF_2026-07-11_s28.md`; Exit A achieved.**

## Minimal read

Read this file and attack-note §21. Consult the finite pentagon oracle
only for state enumeration. The spaced-master relay is dead; do not
repair its infinite completion before replacing its binary trigger.

## Killed candidate and lesson

The relay/free-ultrafilter state extends across the block paste, but the
absorbing regime is already forced by $x_0=x_2=1$. No σ-state realizes
that pair, so σ-states falsely impose $x_0\le x_2^\perp$ and fail to
order-determine the OML. An admissible concrete σ-class representation
is impossible.

Any future infinite master block must explicitly be the complete
Boolean algebra $\mathcal P(\mathbb N)$; a bare countable Greechie block
is only finite–cofinite and not σ-complete.

## Priority 1 — essential ternary trigger census

Search finite concrete OML gadgets for elements $C_1,C_2,C_3,H$ such
that:

1. some two-valued state charges all $C_i$;
2. every state charging all three has $H=1$ (or target $D=H^\perp=0$);
3. for each proper subset $S\subsetneq\{C_1,C_2,C_3\}$ there are states
   charging $S$ with $H=0$ and with $H=1$;
4. the $C_i$ are pairwise incompatible and the implication is not a
   shared-tail/carrier equality; and
5. the gadget is an OML, preferably a linear Greechie diagram of girth
   at least five.

Condition 3 is the essentiality/separation screen. Begin with the
pentagon state table; if absent, exhaust linear 5/6-cycle gadgets before
larger diagrams.

Proof-side alternative: show any finite OML exact-one implication with
three inputs has a proper subinput that already forces the output, for a
clearly bounded diagram class.

Exit A: explicit essential ternary OML trigger with exhaustive state
certificate. Exit B: no-trigger theorem/census for a bounded class.
Only after Exit A should countable propagation/master attachment reopen.

## Rolling cycle

Use one exhaustive-search agent and one structural-obstruction agent.
Keep edits centralized, propagate attack note → taxonomy → shovel/live
pointers → next handoff, and validate JSON/diff. The continuing goal
remains active.

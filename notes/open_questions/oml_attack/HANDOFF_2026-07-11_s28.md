# OML lattice attack — session 28 handoff

**SUPERSEDED by `HANDOFF_2026-07-11_s29.md`; indirect relay found.**

## Minimal read

Read this file and attack-note §§21–22. The essential-trigger search is
closed positively; do not repeat the pentagon census.

## Banked finite cell

The pentagon contains 45 essential ternary triggers. Use the
master-ready one

\[
(C_1,C_2,C_3,H)=(a_1,a_3^\perp,a_7^\perp,a_9^\perp),
\]

so the full input forces the atom $D=a_9$ to zero. Every proper input
subset permits both $D=0$ and $D=1$, and the inputs are pairwise
incompatible. This clears the finite σ-state separation screen that
killed the binary relay.

## Priority 1 — composable ternary architecture

Find a finite composition/relay using essential ternary cells that:

1. propagates three genuinely essential input signals to a fresh
   ternary input or kills a fresh target atom;
2. has no proper input subtrace that enters an absorbing all-target-zero
   regime;
3. self-composes without induced three- or four-loops;
4. keeps killed target atoms sufficiently separated for attachment to a
   complete master block $\mathcal P(\mathbb N)$; and
5. retains explicit escape state paths for every proper finite cluster
   trace, sufficient for σ-state order separation.

Search two-cell pentagon identifications first, with exhaustive state
tables and incidence girth. Proof side: derive a composition invariant
showing when ternary essentiality survives or collapses to a binary
trigger.

Exit A: a composable essential ternary repeater with state/girth
certificate. Exit B: complete no-go census for two-cell composites and
the first uncovered size. Exit C: structural collapse theorem for a
bounded gluing class.

## Guardrails

Finite essentiality alone is not an infinite witness. Test proper-input
escape after composition, not only within one cell. The master block
must be the complete atomic Boolean algebra, not finite–cofinite.
Canonical representation on all f.a. states is inadmissible; eventual
σ-states must order-determine the carrier.

## Rolling cycle

Use one exhaustive composition-search agent and one structural agent.
Centralize edits and propagate attack note → taxonomy → shovel/live
pointers → next handoff. Validate JSON and diff. Keep the continuing
goal active.

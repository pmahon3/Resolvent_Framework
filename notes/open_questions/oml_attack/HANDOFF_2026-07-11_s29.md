# OML lattice attack — session 29 handoff

**SUPERSEDED by `HANDOFF_2026-07-11_s30.md`; relay killed at check 4.**

## Minimal read

Read this file and attack-note §23. Consult §22 for the canonical trigger
state only. The finite relay search has a winner; certification now has
priority over larger gadget search.

## Candidate

Use pentagon cells with master-ready root inputs
$(a_1,a_3,a_7)=(1,0,0)$ and target $D=a_9$. Glue

\[
a_3^n=a_7^{n+1},\qquad a_8^n=a_4^{n+1}.
\]

The full state propagates a fresh canonical ternary input and kills each
fresh target. Two-cell composites have 36 states; every proper root
input subset permits the next target both values. Tested chains through
six cells remain linear of girth five. Adjacent targets are at
block-distance four, so a complete master block closes five-cycles.

## Priority — certify four checks

For the infinite chain with all targets $D_n=a_9^n$ in one explicitly
complete atomic master block $\mathcal P(\mathbb N)$:

1. prove or refute latticehood and σ-completeness of the completed paste;
2. extend the free master ultrafilter plus full relay pattern globally;
3. construct principal-master σ-state escape paths for arbitrary finite
   local traces compatible with the abstract order; and
4. prove σ-states order-determine the entire carrier, or exhibit one
   false order pair.

The decisive comparison with the killed binary relay is check 4. Proper
root-subtrace escape removes its known false pair but does not prove
global order separation.

## Search facts and guardrails

Fresh explicit three-port two-cell relays are exhausted: none of 24 has
girth five. Do not confuse this with the indirect two-overlap winner.
The complete master block must be substituted explicitly; a bare
countable Greechie block is finite–cofinite. Finite girth and state
tables do not establish σ-completeness or concrete representability.

## Rolling cycle

Split agents into checks 1–2 and 3–4. Centralize edits; propagate attack
note → taxonomy → shovel/live pointers → next handoff and validate JSON
and diff. Keep the continuing goal active.

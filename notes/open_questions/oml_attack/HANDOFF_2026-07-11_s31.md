# OML lattice attack — session 31 handoff

**SUPERSEDED by `HANDOFF_2026-07-11_s32.md`; candidate interpretation corrected and class killed.**

## Minimal read

Read this file and attack-note §25. The ternary route is closed. Work on
the all-odd removable-state relay only.

## Candidate

In each pentagon let $s_*$ have support
$\{a_1,a_3,a_5,a_7,a_9\}$. The five-block cluster of those odd atoms
uniquely selects $s_*$; removing it leaves the other ten states
order-determining.

Glue cells by

\[
a_1^n=a_5^{n+1},\qquad a_7^n=a_3^{n+1}.
\]

Take target $D_n=a_0^n$. The free path $s_*,s_*,\ldots$ kills every
target. All eleven local states are σ-live for exactly-one target paths,
so local order determination is maximal. Every $D_n$ may lie in one
complete master $\mathcal P(\mathbb N)$; the shortest master cycle has
length five.

## Priority 1 — cross-cell separation

Build the finite path/product automaton and determine whether
principal-master paths separate all non-orders between elements in cells
at arbitrary offsets. Reduce atom/coatom cases as in §24. Continue
offsets until the finite relation stabilizes and report the bound.
Include overlap identifications.

If separation fails, give the first offset and explicit false order.
If it passes, certify global girth, completeness with the literal master,
free/principal state extension, and representation on σ-states.

Exit A: all checks pass—write the full T4 counterexample. Exit B:
explicit cross-cell false order and candidate kill.

## Guardrails

All-local-states-live is necessary, not sufficient. The cluster has five
blocks; this is valid for T4/B′(i), though not the minimal three-block
locus. Preserve the distinction between the complete master and a
finite–cofinite Greechie block.

## Rolling cycle

Use one product-automaton agent and one structural auditor. Centralize
edits, propagate attack note → taxonomy → shovel/live pointers → next
handoff, and validate JSON/diff. Goal remains active.

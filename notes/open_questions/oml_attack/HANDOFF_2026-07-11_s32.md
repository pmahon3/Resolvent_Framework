# OML lattice attack — session 32 handoff

## Minimal read

Read this file and attack-note §26. Section 25 contains a corrected
historical diagnostic; do not revive its initial all-11-live candidate.

## Corrected screen

For a removable cluster state $s_*$ and target atom $D$, a relay must
simultaneously satisfy:

1. $s_*$ has an all-zero-$D$ infinite path (the free state);
2. $s_*$ is not on any exactly-one-$D$ path (otherwise T4 holds); and
3. the exactly-one-live states order-determine the local OML.

All 4,050 oriented two-atom pentagon maps have been exhausted. After
girth and free/nonlive filters, 45 remain; zero pass local order
determination. This class is closed.

## Priority 1 — next finite architecture

Run the corrected screen on either:

1. two-pentagon gluings with three or more atom identifications that
   remain linear/girth at least five; or
2. three-pentagon relays built from one/two-port interfaces.

The full cluster is the five odd atoms selecting the removable all-odd
state; target may be any even atom under symmetry. Apply filters in this
order: incidence validity/girth, free fixed/basin path, cluster-state
non-liveness, local order determination, adjacent/cross-cell separation,
then master geometry. Stop a candidate at its first failure.

Co-equal route: census a larger finite Greechie OML for a removable face
(not merely one removable state) that can be isolated by a finite
pairwise-incompatible cluster. A richer removable face may avoid the
pentagon's liveness/separation tradeoff.

Exit A: relay passing corrected local and bounded cross-cell screens.
Exit B: exhaustive no-go for a sharply bounded next class. Exit C:
larger finite OML removable face with explicit order-determining
complement and cluster isolation.

## Rolling cycle

Use one higher-overlap/three-cell search agent and one removable-face
structural agent. Centralize edits and propagate attack note → taxonomy
→ shovel/live pointers → next handoff. Validate JSON/diff. Keep goal
active.

# OML lattice attack — session 35 handoff

## Banked result: first operative richer-FACE class is a bounded no-go

The 7-loop exemplar `C={a0,a3,a11}` has 29 states and a removable
three-state face; its 26-state complement order-determines all 714
canonical nonorders.  The operative relay machinery is now built and
independently checked under the s34 one-sided rooted/any-position
σ-liveness convention.

Important target correction: all three face states charge `a0`, so `a0`
cannot be the target.  Their common-zero atoms are
`{a1,a2,a4,a10,a12,a13}`.  Cluster-stabilizer symmetry gives three target
orbits: `{a1,a13}`, `{a2,a12}`, `{a4,a10}`.

The exhaustive period-one census of every oriented injective width-1,
width-2, and width-3 atom port is complete:

- `k=1`: 196 maps/target, all two-cell-girth-valid, zero operative;
- `k=2`: 16,562 maps/target, 9,408 valid, zero operative;
- `k=3`: 794,976 maps/target, 122,500 valid; operative counts by target
  `a1,a2,a4,a10,a12,a13` are `2,9,0,0,9,2`.

Every operative pair has the whole face free and non-σ-live and an
order-determining live complement.  Downstream, the four `a1/a13` pairs
fail at three-cell girth.  The remaining 18 form nine reflection orbits
and pass girth through six cells, but all fail master geometry already at
gap one: `d_1=2` or `3`, while the complete master block requires
`d_1>=4`.  Final candidate count: zero.

This is a rigorous **bounded Exit B** for the named exemplar at period one
and widths `k<=3`.  It does not cover `k>=4`, periods `p>=2`, other
7-loop faces, or the two-pentagons-sharing-one-atom cell.

## Independent checks

- generalized core anchors: 29 states, exact three-state face, complement
  order determination;
- explicit graph-oracle liveness samples agree with the production core;
- exhaustive counts independently reproduced on target representatives
  `a1,a2,a4` and transferred by proved cluster-stabilizer reflection;
- all 22 local survivors independently pass the face-free gate;
- quotient girth and master-gap calculations are in the verification
  artifacts and independently audited.

Files: `../verification/census_2026-07-12_s35/` and
`../verification/audit_s35_7loop_census.py`,
`../verification/verify_s35_survivor_girth.py`.

## Next bounded classes

Priority order:

1. period-one width `k=4` on the same exemplar, using pair-distance and
   face-preservation prefilters before dynamic analysis;
2. if infeasible or empty, period-two width `k<=2` with target-orbit and
   interface symmetries;
3. co-equally, screen a high-slack face in the two-pentagons-sharing-one-
   atom cell, where faces reach size ten.

Do not claim the richer-FACE route closed: only the sharply bounded class
above is closed.

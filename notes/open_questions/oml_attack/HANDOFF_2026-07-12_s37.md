# OML lattice attack — session 37 handoff

## Banked result: 7-loop period-two width two is exhaustively empty

For the 7-loop removable face `C={a0,a3,a11}`, exhaust every ordered pair
of individually valid width-two interfaces:

- 9,408 valid labeled ports;
- `9,408^2 = 88,510,464` ordered period-two pairs;
- target representatives `a1,a2,a4`: operative counts `0,0,0`;
- face-stabilizer reflection transfers zero to `a13,a12,a10`.

The operative screen is already empty, so three-cell girth, master
geometry, rooted separation, and cross-cell separation cannot restore a
candidate.  This is a rigorous bounded no-go for the named face at period
two, width two.  Combined with s36, period-two widths `k<=2` are closed.

## Validation

The production C++ engine reconstructs the 29 states and exactly 9,408
valid ports, then runs the exact two-phase monotone bitset fixpoints over
all 88.5M ordered pairs.  Its completed checkpoint records all 9,408
first-port rows and no survivors.

An independent Python implementation agrees with `relay7_core` on:

- all 230,496 period-two width-one map/target cases; and
- 6,000 deterministic period-two width-two map/target samples,

with zero mismatches in `E0`, `E1`, rooted reachability, any-position
`live`, `free`, or operative status.  It also confirms that `E1[0]` and
`live[0]` differ in real cases, so the rooted-order guardrail remains.

All 9,408 complete successor signatures are distinct.  Exact signature
bucketing therefore gives no compression; coarser bucketing requires a
proof or exact reconstruction and recheck.  Phase swap remains unsafe
because phase zero is the distinguished root.

## Files

- production: `../verification/census_2026-07-12_s37/`;
- optimizer audit: `../verification/audit_s37_p2_optimizer.py` and
  `audit_s37_p2_optimizer_RESULTS.md`;
- generic downstream oracle: `../verification/audit_s37_downstream_*`.

## Next iteration

The next cheaply exhaustive class is period three, width one:
`196^3 = 7,529,536` ordered interface triples before incidence gates.
Run it with the now-mandatory root-cell gate.  In parallel, extract a
candidate rooted-separation obstruction from the s36 failures and the
complete s37 zero distribution; use the period-three census to falsify or
sharpen it.  Larger brute-force jumps (period two width three or period one
width five) should wait for a structural reduction.

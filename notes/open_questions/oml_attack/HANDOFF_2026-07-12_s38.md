# OML lattice attack — session 38 handoff

## Banked result: period-three width one is exhaustively empty

For the 7-loop removable face `C={a0,a3,a11}`, all
`196^3 = 7,529,536` ordered triples of labeled width-one interfaces have
been screened with exact rooted three-phase fixed points.  Operative counts
for target representatives `a1,a2,a4` are `0,0,0`; reflection transfers the
result to `a13,a12,a10`.  The mandatory root-cell `E1[0]` gate is implemented,
but no triple survives long enough to reach it.

The sequential failure counts reveal a stable obstruction.  Depending on
target, about 1.48–1.50M triples first fail face freedom; of the remainder,
6.03–6.05M first fail face nonliveness.  Only 375–405 per target satisfy both,
and every one loses complement order determination.  Hence width-one
periodicity through period three does not escape the local liveness/order
tradeoff.

## Validation

The production C++ engine anchors the 29 states and completes all first-port
rows.  A separately written Python bitset implementation agrees with the
established generic oracle on `E0`, `E1`, rooted reachability, `live`, `free`,
and screen status for 1,000 deterministic triples across all six targets
(6,000 comparisons), with zero mismatches.

Artifacts:

- production: `../verification/census_2026-07-12_s38/`;
- independent audit: `../verification/audit_s38_p3_optimizer.py` and
  `audit_s38_p3_optimizer_RESULTS.md`.

## Follow-up classification (completed 2026-07-12)

The 375–405 near misses are now retained individually.  All 1,165 first fail
order separation at root phase zero, and every one loses the same nonorder
`a3 ≰ a1`, independently of target.  Since `a3` is a face premise, this is
a depth-zero root; bounded right-Sasaki enumeration through depth four adds
no new element to `{a0,a3,a11}`.  See the schema-2 checkpoint,
`classify_near_misses.py`, and `sasaki_root_audit.py` in the s38 census.

## Next iteration

Do not jump directly to period-two width three or period-one width five.
The target-independent rooted obstruction is now proved for this census.
Do not infer a general rooting theorem from it.  The higher-slack
two-pentagons-sharing-one-atom cell remains the best distinct architecture.

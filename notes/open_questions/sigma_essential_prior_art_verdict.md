# σ-essential contextual state: hostile prior-art verdict — **NOT plainly open; status under assessment**

> **✅ STATUS (2026-06-20, primary-source-adjudicated — converged).** The two bridges
> the 2026-06-19 banner flagged as unverified have now been checked against
> Derr–Williamson's actual Definitions 4.4 / D.5 and Theorems 4.5 / D.6 (PDF pp.14,
> 48). Converged verdict — **NOT plainly open; settled NEGATIVELY in the
> Polish-representable case; residue is a descriptive-set-theory / σ-Loomis–Sikorski
> question.** Do not swing this further (the verdict oscillated
> uninhabited→cond-closed→open→here; this is the stable point).
> - **Bridge (i) RESOLVED — the needed direction HOLDS.** Cell-emptying needs only
>   *σ-extendable ⟹ non-contextual*, and that holds: a point ω gives δ_ω, a two-valued
>   σ-additive dispersion-free state; a ctbly-add ν on Borel(Ω) with ν|_{D_σ}=w makes
>   w=∫δ_ω dν a barycentre of S_df^σ ⟹ non-contextual. (The converse fails — dispersion-free
>   states ⊋ point-evals — but the converse is NOT in the cell-emptying chain. The
>   2026-06-19 worry "different objects ⟹ D–W misses" was a non-sequitur: it refuted
>   the unused direction.)
> - **Bridge (ii) is the REAL (and sole) limit.** D.6's Polish + σ(D_σ)=Borel(Ω) +
>   inner-regular hypotheses are all on the underlying POINT SET Ω. D–W *presuppose* a
>   Polish Borel realisation; for an abstract concrete σ-complete OML, producing such an
>   Ω is itself the σ-Loomis–Sikorski problem. So D.6 empties the cell exactly for
>   Polish-representable OMLs; a witness must be NON-Polish-representable. That residue
>   = the σ-LS wall, DST-flavoured (rhymes with Strategy D's ZFC-independence).
> Recorded in oml_onboarding.tex Rmk `rem:dw` (refined 2026-06-20). The
> "conditionally SOLVED-NEGATIVE" header below is essentially right; read "conditional"
> as "in the Polish-representable case." (Author attribution "Fröhlich" was WRONG →
> **Derr & Williamson**, Tübingen.)
>
> **Maharam 1972 §8 read directly (2026-06-20, `maharam_1972.pdf` = the source D–W
> Thm D.6 leans on).** The topological hypothesis is **load-bearing** — confirms the
> residue is real and stable: §8.1 shows a topology-free σ-extension always exists but
> only on the *enlarged Stone space* (ultrafilters of the field → phantom points), not
> the original points; §8.2 (Thm 8.1) keeps it on the original space only under
> **Hausdorff + inner-regularity** (8.1) + a positivity condition (8.2). So the open
> sub-question "is the topological hypothesis removable?" = **NO** (removing it lands
> on the wrong, enlarged object — the f.a.-vs-σ gap, by analogy not identity: Maharam's
> Stone-space-of-a-*field* vs the OML dual, which has no global field). NOT recordable
> / unverified (USER's math call): (a) whether D–W's *Polish* (stronger than Maharam's
> Hausdorff+regular) is reducible in D–W's σ-block-*pasting* port — that's in their
> proof, unread; (b) the sharper residue conjecture — since `S_df` weak-* IS compact
> Hausdorff, the binding residual condition may be **inner-regularity + Borel-σ-generation
> on the natural `S_df` representation**, not "Polish" per se, sharpening "witness must be
> non-Polish-representable" to "fails regularity/Borel-σ-gen on its own df-representation."
> STOP-CHASE noted: both load-bearing sources (D–W, Maharam) now read; verdict held
> across both; Kellerer/embedding-ref are diminishing-returns regress.

*Hostile prior-art search, 2026-06-19. Supersedes the "GENUINELY-OPEN" framing of
`reading1_prize_reduction.md` / `sigma_essential_nonemptiness_finding.md` to the
extent of "not plainly open" (the conditional-negative is unverified — see banner).
Three parallel literature sweeps (Czech/Slovak school, contextuality/gluing,
barycentric/Choquet) + a sub-agent read of the killer paper's verbatim theorem.
Companion: `czech_school_prior_art_sigma_essential.md` (the Czech-school sweep,
which on its own found no killer — the killer came from the imprecise-probability /
Dynkin-system literature).*

> **VERDICT: conditionally SOLVED-NEGATIVE.** The σ-essential cell is **empty** for
> every candidate that admits a Polish Dynkin-system representation with
> `σ(Dσ)=Borel` and inner-regular σ-blocks — proved by **Derr–Williamson et al.,
> arXiv:2302.03522, Theorem D.6**. This paper is the Gaifman-analog: a 2023 result in
> a non-quantum-logic venue (imprecise probability / coherent previsions) that
> answers the exact conjunction. The "open problem" survives **only** inside a
> precise descriptive-set-theory crack (below). It must NOT be presented as plainly
> open; the honest framing is a *double-boxed conditional*.

## The exact target, restated cleanly

The reduction's object = **a σ-additive state on a concrete σ-complete OML that is
FA-extendable but NOT σ-extendable** to a global hidden-variable joint. Equivalently:

- the programme's "non-contextual" = `w` in the **closed convex hull of dispersion-free
  states** = a **countably-additive (σ-additive)** mixture of two-valued states = a
  σ-additive global joint. (Confirmed from `reading1_prize_reduction.md` lines 68–74:
  the ∏ₙMO₂ analysis treats a *finitely-additive* diffuse state as contextual and a
  *σ-additive* state as forced into the hull. So the hull is the σ-mixture hull.)
- "contextual but finite-restrictions-classical" (clause b) = **a finitely-additive
  global joint exists** (every finite sub-OML has a local joint; these glue to an FA
  global joint by Horn–Tarski/Rao–Rao).

So **σ-essential contextual state = Yosida–Hewitt / Maharam gap object**: an FA global
joint exists, but no σ-additive one. That is the entire content of the cell.

## The killer paper — verbatim, verified from the PDF

**B. Derr & Williamson, "Systems of Precision: Coherent Probabilities
on Pre-Dynkin-Systems and Coherent Previsions on Linear Subspaces," arXiv:2302.03522
(2023).** Pre-Dynkin-systems = concrete OMPs (set-representable); Dynkin-systems = the
σ-complete concrete ones; σ-blocks = the maximal Boolean sub-σ-algebras. The two
load-bearing theorems (read verbatim from the arXiv PDF, not an abstract):

- **Theorem 4.5 (Horn–Tarski / Rao–Rao 3.2.10).** A finitely additive probability `µ`
  on a pre-Dynkin-system `D` is **extendable to `2^Ω`** iff
  `Σχ_{B_k} − Σχ_{A_j} ≥ 0 ∀ω ⟹ Σµ(B_k) − Σµ(A_j) ≥ 0` for **all finite families**
  of sets in `D`. Extension to `2^Ω` = an FA global joint = an FA mixture of
  dispersion-free states. **RHS = clause (b).** (The finite-family condition is exactly
  "non-contextual on every finite sub-OML": a finite gamble family touches only finitely
  many `µ`-values, so it is a finite/local condition.)

- **Theorem D.6 (σ-Extendability).** Let `Fσ` be the Borel σ-algebra on a **Polish**
  space `Ω`, `Dσ ⊆ Fσ` a Dynkin-system with **`σ(Dσ)=Fσ`**, and assume each σ-block
  restriction `µ_i := µσ|A_i` is **inner regular**. Then `µσ` is **σ-extendable**
  (extends to a countably-additive measure on `Fσ`) **iff** `Σ∫f_j dµ_{i_j} ≥ 0`
  whenever `Σf_j ≥ 0`, over all finite families of simple gambles. (Proof: Prop. D.4
  splits the Dynkin space into Kolmogorov spaces ⟹ Maharam 1972 Thm 8.1 ⟹ measure on
  the generated algebra ⟹ Carathéodory ⟹ measure on `Fσ`.)

**The boxing fact** is D.6-over-4.5: the D.6 RHS *is* the 4.5 RHS (gamble form = set
form linearized). So under (Polish, `σ(Dσ)=Borel`, inner-regular blocks):

> **FA-extendable (clause b) ⟹ σ-extendable.** The Yosida–Hewitt gap **closes**.
> Hence clause (b) ⟹ σ-additive global joint ⟹ **non-contextual**. The σ-essential
> cell is **empty** wherever D.6 applies.

### Sanity checks (both pass — D.6 is being read correctly)

- **Wright's pentagon:** finite ⟹ Polish, inner-regular; contextuality is
  finitely witnessed ⟹ clause (b) **fails** ⟹ 4.5/D.6 RHS fails ⟹ correctly
  comes out **contextual**. ✓ (Does NOT contradict D.6 — D.6 only kills the cell
  where (b) holds.)
- **∏ₙMO₂ on Ω=ℕ:** discrete ⟹ Polish, atomic ⟹ inner-regular; (b) holds ⟹ D.6
  forces σ-extendable ⟹ **non-contextual**, reproducing the programme's prior
  hand-kill ("σ kills its contextuality through the center"). ✓

## The surviving crack — a descriptive-set-theory question (the new frontier)

D.6 is **representation-dependent**. To wield it as a kill on a given concrete
σ-complete OML `L`, you must represent `L` as a Dynkin-system on a **Polish** `Ω`
with `σ(Dσ)=Borel` and inner-regular blocks. The canonical representation is
`Ω = S_df^σ(L)` — the σ-additive dispersion-free states (the separating set
concreteness supplies), weak-\* topology. In that representation D.6's "σ-extendable"
= the intrinsic "non-contextual." So the entire residual open question is:

> **Is `S_df^σ(L)` (weak-\* topology) Polish / standard-Borel, with `L` generating
> its Borel σ-algebra (`σ(Dσ)=Fσ`)?**

- If **always yes** (every concrete σ-complete OML admits such a rep) → D.6 always
  applies → the cell is **unconditionally empty** → SOLVED-NEGATIVE, problem dead.
- If **not always** → a witness can live **only** where this fails: `S_df^σ` is
  **analytic-but-not-Borel / non-Polish**. σ-additive states are generally NOT
  weak-\* closed, so this is genuinely plausible — and is the *only* crack.
  Inner-regularity is **secondary**: on a Polish `Ω`, Ulam's theorem makes Borel
  probability measures Radon, so footnote-29 inner-regularity is near-automatic; the
  **binding** hypotheses are **Polish + Borel-generation**.

This is the exact rhyme with Strategy D / Gaifman: there the residue was
ZFC-independence; here it is **descriptive set theory** (Polish vs. analytic). The
"off-center / non-band / pasting" constraints from Sessions 1–8 are *subsumed and
sharpened* by this single representability question.

## What must still be read to be fully sure (hostile-referee obligation)

The residual inhabitation question is now **pure measure theory**: does the
FA-but-not-σ-extendable gap occur for λ-systems / Dynkin-systems **off** the Polish
regime? A killer paper (either direction) could sit in:

- **Maharam 1972, "Consistent extension of linear functionals and of probability
  measures," 6th Berkeley Symp.** — is the Hausdorff/Polish hypothesis of her Thm 8.1
  necessary, or is there a regime-free version? (D.6's whole σ-strength rests on it.)
- **Kellerer 1964** (countably-additive marginal gluing) and the marginal-problem
  literature: **Strassen 1965**, **Shortt**.
- **Vorob'ev 1962** (already in MEMORY as the Strategy-D Gaifman-analog) — finite,
  surjective-restriction; near-miss but the canonical local-to-global root.

## Other near-misses found by the three sweeps (none closes the cell)

- **Barbosa–Douce–Emeriau–Kashefi–Mansfield, "Continuous-Variable Nonlocality and
  Contextuality," CMP 391 (2022); arXiv:1905.08267** — σ-additive sheaf-theoretic
  global sections (clears axis a), BUT marginal/no-signalling setting with
  **surjective** restriction maps; does not address infinite-only contextuality
  (clause b). Near-miss; a referee will reach for it — distinguish by the
  non-surjective-restriction point.
- **Navara–Rüttimann, "A characterization of σ-state spaces of OMLs," Expo. Math. 9
  (1991) 275–284** — characterizes the σ-additive state space, BUT only as a
  **semi-exposed face** of the full state space (verbatim via Zbl 0742.03027, Pták's
  review); forces no simplex, says nothing about dispersion-free vertices. Does not
  close.
- **Navara–Pták, "Two-valued measures on σ-classes," Čas. Pěst. Mat. 108 (1983)
  225–229** — σ-classes ARE σ-complete concrete logics (axis a ✓), but the whole paper
  is about two-valued (= dispersion-free, non-contextual-by-definition) measures.
  Rejected at the precondition. Its closing finite example fails (b).
- **de Simone–Navara–Pták symmetric-difference / EQL line** (Math. Nachr. 288 (2015);
  arXiv:2401.13651) — the band/difference-closed subclass, **finite** non-spanning
  examples; fails (b). This is the family Session 8 found CLOSED (DEATH 2 dichotomy);
  the literature agrees it is finitely controlled.
- **Realization theorems** (Shultz 1974; Navara–Rogalewicz 1988; Harding–Navara 2000)
  — realize arbitrary compact convex sets as OML state spaces (SOLVED-POSITIVE risk),
  but the realized space is the **finitely-additive** state space, constructions are
  finite-pasting/Greechie (generically not σ-complete, not known-concrete). No located
  paper realizes a non-simplex state space on a **concrete AND σ-complete** OML.
- **Gudder 1968, "Dispersion-Free States and the Exclusion of Hidden Variables," Proc.
  AMS 19:319** — block-extension of dispersion-free states; finite/algebraic, not
  σ-additive-infinite. Origin of the framing, not a solution.

## Bottom line for the survey

Do **not** present this as a clean open problem. The defensible statement is:

> *Conditionally settled negative.* Derr–Williamson (2023, arXiv:2302.03522,
> Thm D.6) shows the σ-essential cell is empty for any concrete σ-complete OML
> representable as a Dynkin-system on a Polish space with Borel-generating blocks. The
> only remaining room for a witness is descriptive-set-theoretic: a concrete σ-complete
> OML whose σ-additive dispersion-free state space is non-Polish / analytic-not-Borel.
> Whether that can occur — and whether Maharam's Polish hypothesis is removable — is the
> precise open residue, to be settled by reading Maharam 1972 Thm 8.1 and the
> countably-additive marginal-gluing literature (Kellerer 1964, Strassen 1965).

\noindent The object: a concrete $\sigma$-complete non-Boolean irreducible OML carrying a
finite two-valued state on a $\perp$-closed subposet that extends to no global
$\sigma$-additive two-valued state. Localized to $\sigma$-point-selection failure; frontier
is non-Polish / non-standard-Borel / irreducibly non-central. Sources in dependency order,
each with the question to read it for.

\section{Descriptive set theory}

\noindent\textbf{Kechris, \emph{Classical Descriptive Set Theory} (GTM 156).} Borel /
analytic / co-analytic hierarchy $\to$ Borel equivalence relations $\to$ the
Glimm--Effros dichotomy; then uniformization.
\begin{itemize}[leftmargin=*]
  \item State Glimm--Effros precisely (non-smooth $\iff$ $E_0$ embeds $\iff$ no Borel
    two-valued transversal). Which word does not transport to clause~(ii)? (``Borel.'')
  \item Co-analytic uniformization-failure: why is its obstruction real-valued / over a
    standard-Borel base, hence Derr--Williamson-killed?
\end{itemize}

\noindent\textbf{Gao, \emph{Invariant Descriptive Set Theory}} (after Kechris).
$E_0$ / $E_\infty$ / hyperfinite / turbulence.
\begin{itemize}[leftmargin=*]
  \item Define hyperfinite; state Adams--Kechris. Why is $E_\infty$ non-hyperfiniteness
    orthogonal to clause~(ii)?
\end{itemize}

\section{Large cardinals and forcing}

\noindent\textbf{Jech, \emph{Set Theory} (3rd millennium ed.).}
\begin{itemize}[leftmargin=*]
  \item Filters, ideals, ultrafilters: why is $\mathcal{F}_s = \{A : s(A)=1\}$ not a filter
    on a non-Boolean concrete OML, even for a Dirac state? ($MO_2$:
    $a\cap b\neq\varnothing$ as sets yet $a\wedge b = 0$.)
  \item Measurable cardinals, Ulam matrices: re-derive the matrix by hand; locate the line
    needing non-orthogonal unions / comprehension.
  \item Forcing: why does set-forcing preserve measurability (L\'evy--Solovay)?
\end{itemize}

\noindent\textbf{Kanamori, \emph{The Higher Infinite}} (after Jech, if the strength path
calls). Measurable $\to$ ultrapower $\to$ elementary embedding $j\colon V\to M$.
\begin{itemize}[leftmargin=*]
  \item The UB route transports $U$ along $j$, but $U$ lives on Boolean
    $\mathcal{P}(\kappa)$; why is transport to orthogonal-only a costume?
\end{itemize}

\section{Primary sources (read for technique)}

\begin{itemize}[leftmargin=*]
  \item \textbf{Navara--Pt\'ak 1983.} Re-derive their ZFC non-Dirac $\sigma$-state. What
    makes $L_{f,g}$ support it, and why is it not a witness?
  \item \textbf{Derr--Williamson 2023 $+$ Maharam 1972 \S8.} Where is inner-regularity
    load-bearing in Thm~D.6 / Maharam~\S8.2?
  \item \textbf{Blecher--Weaver 2017.} Identify the ultrafilter-from-masa step (Prop.~2.3);
    state the masa-free question it leaves open for the concrete two-valued case.
\end{itemize}

\section{Notes}

\begin{itemize}[leftmargin=*]
  \item Start: Kechris (Borel equivalence relations $\to$ Glimm--Effros).
  \item Definability-vs-existence first: bounds \S3f and \S3j, plus the Lean polarity gate.
  \item Operator algebra (masas, von Neumann algebras) is not on the critical path: the
    masa route is dead, so light functional analysis suffices for Blecher--Weaver.
  \item Chapter numbers are convenience pointers; navigate by topic.
\end{itemize}

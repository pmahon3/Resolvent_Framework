\maketitle

\begin{abstract}
Paper~I produced a probability measure $P$ on the observable $\sigma$-algebra
from coherent structured observations.  This paper asks what temporal structure
$P$ carries.

The classical approach to dynamics, following Koopman, describes how observable
functions evolve under a known transformation $T$.  This presupposes access to
$T$.  The approach here treats prediction as primary: given a current observation
$Q$, what does the observer predict about the future $F$?  This question has a
canonical answer — the \emph{predictive kernel} $\Pi_Q$, the regular conditional
distribution of $F$ given $Q$ — and a canonical minimal form — the
\emph{minimal predictive state map} $Q_*$, sending each sample point to its
conditional law.  The \emph{predictive factorization theorem} shows that $Q_*$
is the minimal sufficient statistic for future prediction.

Indexing over time, the prediction laws compose.  Their composition rule is the
Chapman--Kolmogorov equation, derived here from temporal coherence rather than
assumed.  From it emerge the \emph{predictive operators} $\{K_t\}$, their
semigroup law, and their duality with the measure-evolution operators $\{P_t^*\}$.
When predictions are certain — Dirac kernels — the construction recovers the
classical Koopman operator exactly.

The whole construction assembles into the \emph{observable dynamical system}
$(O_{Q_*}, \nu_{Q_*}, \{K_t\})$.  Setting $Q_t = h \circ T^t$ in a
measure-preserving system identifies $Q_*$ with the delay map $\Phi_h$, making
this the direct input to the reconstruction question of Paper~III.
\end{abstract}

\begin{quote}\small
\textbf{2020 Mathematics Subject Classification.}
37A30 (primary); 47D06, 60J05, 28A99 (secondary).

\textbf{Keywords.}
Koopman operator, predictive kernel, minimal predictive state, Markov semigroup,
Koopman--Perron duality, observable dynamical system, measure-preserving system.
\end{quote}

% ====================================================================
\section{Introduction}
\label{sec:intro}

Paper~I produced a probability measure $P$ on the observable $\sigma$-algebra
from coherent structured observations; collective exhaustion was the exact
condition licensing the passage from finite coherence to genuine probability.
The question this paper addresses is what temporal structure $P$ carries.  Once
the probability is fixed, the laws of motion are already determined.

The classical approach to dynamics, following Koopman~\cite{Koopman1931},
describes how observable functions evolve: the operator $U_T f = f \circ T$
acts by precomposing with the dynamics $T$.  This presupposes access to $T$.
The approach here replaces that assumption with a prior question: given the
current observation $Q : \Omega \to \alpha$, what does the observer predict
about the future observation $F : \Omega \to \beta$?  The answer is the
\emph{predictive kernel} $\Pi_Q$, the regular conditional distribution of $F$
given $Q$.  No state-space transformation is assumed.

The predictive kernel packages into the \emph{minimal predictive state map}
$Q_* : \Omega \to \mathcal{P}(\beta)$, sending each sample point to its
conditional law.  The predictive factorization theorem shows that $Q_*$ is the
minimal sufficient statistic for future prediction: every conditional expectation
of a function of $F$ factors through it.  Indexing over time, the prediction
laws compose; their composition rule is the Chapman--Kolmogorov equation, which
is derived from temporal coherence rather than assumed.  This yields the
\emph{predictive operators} $\{K_t\}$, their semigroup law, and the
Koopman--Perron duality connecting operators on functions with operators on
measures.  When the kernels are Dirac measures, the construction collapses to
the classical Koopman operator.

These structures assemble into the \emph{observable dynamical system}
$(O_{Q_*}, \nu_{Q_*}, \{K_t\})$, which Paper~III takes as its starting point
and asks: when does the delay orbit of a single function reconstruct the full
state space?

% ====================================================================
\section{Setup}
\label{sec:setup}

The paper is self-contained.  The connection to Paper~I~\cite{mahon_paper1} is
motivational; the formalism starts here from a probability space
$(\Omega, \mathcal{F}, P)$ and two measurable maps:
\begin{itemize}
  \item $Q : \Omega \to \alpha$, where $(\alpha, \mathcal{A})$ is a measurable
    space;
  \item $F : \Omega \to \beta$, where $(\beta, \mathcal{B})$ is a standard
    Borel space.
\end{itemize}
The standard Borel hypothesis on $\beta$ is used once: it is the hypothesis
under which the Rokhlin disintegration theorem~\cite{Rokhlin1952} guarantees the
existence of a regular conditional distribution.  All other results hold in
greater generality.

We write $\mathcal{P}(\beta)$ for the space of probability measures on $\beta$,
equipped with its canonical (Giry) $\sigma$-algebra.  The \emph{joint
pushforward} is the measure $\rho := P \circ (Q, F)^{-1}$ on $\alpha \times
\beta$; its marginals are $\rho_\alpha = P \circ Q^{-1}$ and $\rho_\beta = P
\circ F^{-1}$.

The main theorems are formalized in Lean~4 / Mathlib~\cite{Mathlib} in
\texttt{PredictiveState.lean} and \texttt{PredictiveOperators.lean}.  Several
further results — the experiment preorder, general predictive sufficiency, the
continuous-time generator — are stated in the paper but not yet formalized;
these are noted at the relevant points.

% ====================================================================
\section{The predictive kernel and the minimal predictive state map}
\label{sec:predictive}

\subsection{The predictive kernel}

\begin{definition}[Predictive kernel]
\label{def:predictive-kernel}
The \emph{predictive kernel} $\Pi_Q : \alpha \to \mathcal{P}(\beta)$ is the
regular conditional distribution of $F$ given $Q$:
\[
  \Pi_Q(q, A) \;=\; P(F \in A \mid Q = q),
\]
defined as the disintegration kernel $\Pi_Q(q, \cdot) = \rho(\cdot \mid
\alpha)$ of the joint pushforward $\rho = P \circ (Q, F)^{-1}$ over its first
marginal.
\end{definition}

Concretely: $\Pi_Q$ is the unique (up to $P \circ Q^{-1}$-null sets) Markov
kernel $\alpha \to \mathcal{P}(\beta)$ such that
\[
  \int_A \Pi_Q(q, B)\, d(P \circ Q^{-1})(q) \;=\; P(Q \in A,\; F \in B)
\]
for all measurable $A \subseteq \alpha$ and $B \subseteq \beta$.

\begin{proposition}[Predictive compatibility]
\label{prop:predictive-compat}
Let $\pi : \alpha \to \alpha'$ be measurable with $Q' = \pi \circ Q$.  Then
for $(P \circ Q^{-1})$-almost every $q \in \alpha$:
\[
  \Pi_Q(q, \cdot) \;=\; \Pi_{Q'}(\pi(q), \cdot).
\]
\end{proposition}
\begin{proof}
The joint pushforward $\rho' = P \circ (Q', F)^{-1}$ satisfies $\rho' = \rho
\circ (\pi \times \mathrm{id})^{-1}$.  The claim follows from the
$P \circ Q^{-1}$-a.e.\ uniqueness of the disintegration of $\rho'$ over its
first marginal, combined with the covariance of condKernel under measurable
maps (\texttt{eq\_condKernel\_of\_measure\_eq\_compProd} in Mathlib).
\end{proof}

\subsection{The predictive law map and the minimal predictive state map}

The predictive kernel $\Pi_Q$ maps each value $q \in \alpha$ to a probability
measure on outcomes.  Packaging this as a single measurable map gives:

\begin{definition}[Predictive law map and minimal predictive state map]
\label{def:minimal-predictive}
The \emph{predictive law map} is
\[
  \varphi_Q : \alpha \to \mathcal{P}(\beta), \qquad \varphi_Q(q) = \Pi_Q(q, \cdot).
\]
The \emph{minimal predictive state map} is the composition
\[
  Q_* := \varphi_Q \circ Q : \Omega \to \mathcal{P}(\beta).
\]
The value $Q_*(\omega)$ is the full conditional law $P(F \in \cdot \mid Q =
Q(\omega))$.
\end{definition}

\begin{proposition}[Measurability]
\label{prop:measurability}
The maps $\varphi_Q$ and $Q_*$ are measurable.
\end{proposition}
\begin{proof}
$\varphi_Q$ is the underlying map of the Markov kernel $\Pi_Q$, which is
measurable by definition of a kernel (\texttt{Kernel.measurable} in Mathlib).
Since $\mathcal{P}(\beta)$ carries the subspace $\sigma$-algebra from the
Giry space of all measures on $\beta$, the wrap as a \texttt{ProbabilityMeasure}
is measurable by \texttt{Measurable.subtype\_mk}.  Measurability of $Q_*$ then
follows from composition.
\end{proof}


% ====================================================================
\section{Predictive factorization}
\label{sec:factorization}

The predictive factorization theorem is the central result of this paper.  It
says that the conditional expectation of any bounded measurable function of $F$,
given $Q$, factors through the minimal predictive state map $Q_*$.

\begin{theorem}[Predictive factorization]
\label{thm:predictive-factorization}
For every bounded measurable $g : \beta \to \mathbb{R}$:
\[
  E[g(F) \mid \sigma(Q)] \;=\; \Bigl(\omega \mapsto \int g\, dQ_*(\omega)\Bigr)
  \quad P\text{-a.e.}
\]
\end{theorem}
\begin{proof}
Let $h(\omega) := \int g\, dQ_*(\omega) = \int g\, d\Pi_Q(Q(\omega), \cdot)$.
We verify the two defining properties of the conditional expectation.

\emph{Measurability.}  The map $q \mapsto \int g\, d\Pi_Q(q, \cdot)$ is
strongly measurable (Bochner integrability of $g$ against a Markov kernel;
\texttt{StronglyMeasurable.integral\_kernel} in Mathlib).  Since $Q_* = \varphi_Q
\circ Q$, the map $h = (\text{predictive operator applied to } g) \circ Q_*$
is $\sigma(Q)$-measurable.

\emph{Set-integral identity.}  For every $\sigma(Q)$-measurable set $S =
Q^{-1}(A)$:
\begin{align*}
  \int_S h\, dP
  &= \int_{Q^{-1}(A)} \!\! \int g\, d\Pi_Q(Q(\omega), \cdot)\, dP(\omega) \\
  &= \int_A \int g\, d\Pi_Q(q, \cdot)\, d(P \circ Q^{-1})(q)
  \quad\text{(push-forward)} \\
  &= \int_A \int_{A \times \beta} g(f)\, d\rho(q, f)
  \quad\text{(disintegration identity)} \\
  &= \int_{Q^{-1}(A)} g(F(\omega))\, dP(\omega) = \int_S g(F)\, dP.
\end{align*}
The key step is the disintegration identity for $\rho = P \circ (Q, F)^{-1}$,
which in Mathlib is \texttt{Measure.setIntegral\_condKernel}.

By the a.e.\ uniqueness of conditional expectation
(\texttt{ae\_eq\_condExp\_of\_forall\_setIntegral\_eq}), $h = E[g(F) \mid
\sigma(Q)]$ $P$-a.e.
\end{proof}

\begin{corollary}[Predictive sufficiency]
\label{cor:sufficiency}
$E[g(F) \mid \sigma(Q)] = E[g(F) \mid \sigma(Q_*)]$ $P$-a.e.
\end{corollary}
\begin{proof}
Since $Q_* = \varphi_Q \circ Q$, we have $\sigma(Q_*) \subseteq \sigma(Q)$.
The conditional expectation given $\sigma(Q)$ already factors through $Q_*$
by Theorem~\ref{thm:predictive-factorization}, so it is $\sigma(Q_*)$-measurable.
\end{proof}

The canonical predictive operator $K_{Q_*}$ is the map
\[
  (K_{Q_*} g)(q_*) \;=\; \int g\, dq_*, \qquad q_* \in \mathcal{P}(\beta).
\]
Theorem~\ref{thm:predictive-factorization} then reads: $E[g(F) \mid \sigma(Q)]
= (K_{Q_*} g) \circ Q_*$ $P$-a.e.  This is the canonical operator associated
with the predictive state.

% ====================================================================
\section{The predictive semigroup}
\label{sec:semigroup}

So far, the setup has been static: a single pair of measurable maps $(Q, F)$
representing present and future.  To introduce dynamics, we index over a
discrete time horizon.  For each $t \in \mathbb{N}$, let $F_t : \Omega \to \gamma$ be the
observable at time $t$, and let $\Pi_t$ be the predictive kernel of $F_t$
given the current predictive state.

\subsection{Predictive kernel families}

\begin{definition}[Predictive kernel family and semigroup kernel]
\label{def:kernel-family}
A \emph{predictive kernel family} on a state space $\gamma$ is a family
$\{\Pi_t\}_{t \in \mathbb{N}}$ of Markov kernels $\Pi_t : \gamma \to
\mathcal{P}(\gamma)$ with $\Pi_0 = \delta$ (identity: $\Pi_0(q, \cdot) = \delta_q$).

A \emph{predictive semigroup kernel} is a predictive kernel family satisfying
the Chapman--Kolmogorov equation:
\[
  \Pi_{t+s}(q, \cdot) \;=\; \int_\gamma \Pi_t(q', \cdot)\, d\Pi_s(q, q')
  \quad\text{for all } q \in \gamma,\; t, s \in \mathbb{N}.
\]
In kernel notation: $\Pi_{t+s} = \Pi_t \circ_\kappa \Pi_s$ where $\circ_\kappa$
denotes kernel composition.
\end{definition}

\subsection{The predictive operator family}

\begin{definition}[Predictive operator family]
\label{def:predictive-op-family}
Given a predictive kernel family $\{\Pi_t\}$, the \emph{horizon-indexed
predictive operator} at horizon $t$ is
\[
  (K_t g)(q) \;:=\; \int g\, d\Pi_t(q, \cdot), \qquad g : \gamma \to \mathbb{R}.
\]
\end{definition}

\begin{proposition}[Basic properties]
\label{prop:basic-properties}
For any predictive kernel family $\{\Pi_t\}$:
\begin{enumerate}[label=(\roman*)]
  \item \emph{Identity at zero}: $K_0 g = g$ for all measurable $g$;
  \item \emph{Positive}: $g \geq 0 \Rightarrow K_t g \geq 0$;
  \item \emph{Unital}: $K_t \mathbf{1} = \mathbf{1}$;
  \item \emph{Contractive}: $\|K_t g\|_\infty \leq \|g\|_\infty$.
\end{enumerate}
\end{proposition}
\begin{proof}
Identity at zero: $K_0 g(q) = \int g\, d\delta_q = g(q)$.  Positivity and
unitality are immediate from the Markov property and non-negativity of
integration against a positive measure.  Contractiveness:
$|(K_t g)(q)| = |\int g\, d\Pi_t(q,\cdot)| \leq \|g\|_\infty \cdot \Pi_t(q, \gamma) = \|g\|_\infty$.
\end{proof}

\begin{theorem}[Semigroup property]
\label{thm:semigroup}
Let $\{\Pi_t\}$ be a predictive semigroup kernel.  For every bounded measurable
$g : \gamma \to \mathbb{R}$:
\[
  K_{t+s} g \;=\; K_t(K_s g).
\]
\end{theorem}
\begin{proof}
Using the Chapman--Kolmogorov equation $\Pi_{t+s} = \Pi_t \circ_\kappa \Pi_s$:
\begin{align*}
  (K_{t+s} g)(q)
  &= \int g\, d\Pi_{t+s}(q, \cdot) \\
  &= \int g\, d(\Pi_t \circ_\kappa \Pi_s)(q, \cdot) \\
  &= \int \Bigl(\int g\, d\Pi_t(q', \cdot)\Bigr)\, d\Pi_s(q, q')
  \quad\text{(Bochner--Fubini for kernel composition)} \\
  &= \int (K_t g)(q')\, d\Pi_s(q, q')
  \;=\; (K_t(K_s g))(q).
\end{align*}
The Bochner--Fubini step is \texttt{Kernel.integral\_comp} from
\texttt{Mathlib.Probability.Kernel.Composition.IntegralCompProd}.  The
boundedness hypothesis on $g$ is needed for integrability of $g$ against the
composed kernel.
\end{proof}

% ====================================================================
\section{Koopman--Perron duality}
\label{sec:duality}

The predictive operators $K_t$ act on functions.  The dual objects act on
measures.

\begin{definition}[Koopman--Perron dual]
\label{def:kp-dual}
Given a predictive semigroup kernel $\{\Pi_t\}$ and a (finite) measure $\mu$
on $\gamma$, the \emph{Koopman--Perron push-forward} is
\[
  P_t^* \mu \;:=\; \mu \mathbin{\star} \Pi_t
  \;=\; \int_\gamma \Pi_t(q, \cdot)\, d\mu(q).
\]
In Mathlib notation: \texttt{$\mu$.bind (ker $t$)}.
\end{definition}

\begin{theorem}[Koopman--Perron duality]
\label{thm:koopman-perron}
For every bounded measurable $g : \gamma \to \mathbb{R}$ and finite measure
$\mu$ on $\gamma$:
\[
  \int_\gamma (K_t g)\, d\mu \;=\; \int_\gamma g\, d(P_t^* \mu).
\]
\end{theorem}
\begin{proof}
\begin{align*}
  \int g\, d(P_t^* \mu)
  &= \int g\, d(\mu \mathbin{\star} \Pi_t)
  \;=\; \int g\, d\!\left(\int \Pi_t(q, \cdot)\, d\mu(q)\right) \\
  &= \int_\gamma \int g\, d\Pi_t(q, \cdot)\, d\mu(q)
  \quad\text{(Bochner--Fubini)} \\
  &= \int_\gamma (K_t g)(q)\, d\mu(q).
\end{align*}
The Bochner--Fubini step again uses \texttt{Kernel.integral\_comp}.
\end{proof}

% ====================================================================
\section{Deterministic specialization}
\label{sec:deterministic}

When the dynamics is deterministic, the stochastic picture collapses to the
classical Koopman picture.

\begin{theorem}[Deterministic specialization]
\label{thm:deterministic}
Suppose $\Pi_t(q, \cdot) = \delta_{\varphi_t(q)}$ for a measurable map
$\varphi_t : \gamma \to \gamma$.  Then for every measurable $g$:
\[
  K_t g \;=\; g \circ \varphi_t.
\]
\end{theorem}
\begin{proof}
$(K_t g)(q) = \int g\, d\delta_{\varphi_t(q)} = g(\varphi_t(q))$.  The
integral against a Dirac measure is \texttt{integral\_dirac'} in Mathlib.
\end{proof}

\begin{corollary}[Flow law]
\label{cor:flow-law}
If $\Pi_t(q, \cdot) = \delta_{\varphi_t(q)}$ for all $t$, then
\[
  \varphi_{t+s} \;=\; \varphi_t \circ \varphi_s.
\]
\end{corollary}
\begin{proof}
From $\Pi_{t+s} = \Pi_t \circ_\kappa \Pi_s$ and the Dirac hypothesis:
\begin{align*}
  \delta_{\varphi_{t+s}(q)}
  &= \Pi_{t+s}(q, \cdot)
  \;=\; (\Pi_t \circ_\kappa \Pi_s)(q, \cdot) \\
  &= \Pi_s(q, \cdot) \mathbin{\star} \Pi_t
  \;=\; \delta_{\varphi_s(q)} \mathbin{\star} \Pi_t
  \;=\; \Pi_t(\varphi_s(q), \cdot)
  \;=\; \delta_{\varphi_t(\varphi_s(q))}.
\end{align*}
Since Dirac measures at distinct points are distinct (under
\texttt{SeparatesPoints}), we conclude $\varphi_{t+s}(q) = \varphi_t(\varphi_s(q))$.
\end{proof}

The full structure produced by the construction is named once:

\begin{definition}[Observable dynamical system]
\label{def:obs-dyn-sys}
An \emph{observable dynamical system} is a triple
$(O_{Q_*},\, \nu_{Q_*},\, \{K_t\}_{t \in \mathbb{N}})$
where $O_{Q_*} = \mathcal{P}(\beta)$ is the predictive state space,
$\nu_{Q_*} = P \circ Q_*^{-1}$ is the predictive state distribution, and
$\{K_t\}$ is the predictive operator family satisfying the semigroup law and
Markov properties above.
\end{definition}

% ====================================================================
\section{Relation to Paper III}
\label{sec:bridge}

Setting $Q_t = h \circ T^t$ in a measure-preserving system
$(X, \mathcal{B}, \mu, T)$ with $h \in L^\infty$ specialises the construction
directly.  Under this choice, $Q_* = \Phi_h$, the delay map $\Phi_h(x) =
(h(T^n x))_{n \in \mathbb{Z}}$, and the predictive state space $O_{Q_*}$ is
precisely the delay orbit space.  The observable dynamical system is the input
to Paper~III.

Paper~III asks: when does the delay orbit of a single observable $h$ determine
the full state space?  The answer is the \emph{reconstruction theorem}: the
observable $\sigma$-algebra $\mathcal{O}_h = \sigma(\{h \circ T^n\})$ equals
$\mathcal{B}(X)$ modulo $\mu$ if and only if the algebra generated by
$\{h \circ T^n\}$ is dense in $L^2(X, \mu)$.  The proof turns on the density
bridge: the $L^2$-closure of a function algebra equals the $L^2$-space of the
$\sigma$-algebra it generates.

The Koopman operators $\{U_T^t\}$ connect the two papers.  Paper~II produces
$\{K_t\}$ on $O_{Q_*}$; when $Q_* = \Phi_h$ is injective modulo null sets,
$K_t$ and $U_T^t$ act on the same information.  The cyclic vector condition —
$\overline{\mathrm{span}}\{U_T^n h\} = L^2(X, \mu)$ — is sufficient for
reconstruction but strictly stronger: reconstruction requires only that the
\emph{algebra} generated by the delay orbit be $L^2$-dense, not the linear
span.  They coincide only when $U_T$ has simple spectrum.

When reconstruction holds, $O_{Q_*}$ is the full state space $X$ up to
measure-theoretic identification.  The compact Stone space introduced in Paper~I
as a technical device for measure extension is recognised by Paper~III, under
the reconstruction condition, as $X$ itself.  The programme begins with a
compact completion of the sample space and ends by identifying it as the state
space.

\bibliographystyle{plain}
\bibliography{references}

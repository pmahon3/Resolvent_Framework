# Handoff — reconstruction / universal impossibility (2026-07-09)

Working tree CLEAN, all committed on branch `oml-descent-sigma-essential-reduction`.
Nothing pushed. This session took the reconstruction paper from "proofs owed" through
Phase-4/5 and then opened a serious attack on universal impossibility.

## What happened this session (7 commits, c38c395..47b962e)

1. **Phase-4 proofs written** (`c38c395`) — all 6 PROVE-ORIGINALLY items in the paper
   (parity + PR-box, θ-sweep/signable, forest counting, taming-10, symmetric Circuit
   Localization, winding characterisation). ⟦HAND — unverified⟧.
2. **Phase-5 Lean** (`c38c395`) — `WindingDichotomy.lean`, 0-sorry, certifies the
   flow-decomposition kernel GIVEN the classical flow-decomposition axiom. NOT the
   winding characterisation.
3. **Editorial pass** (`c38c395`) — 12pp → 10pp.
4. **Joint 1 (pruning lemma) CLOSED for all k** (`462eb1a`) — the seed's central open
   joint. The k=2 "phase gap" and k≥3 "permutation gap" were both wrong-proxy
   artifacts, dissolved by labelling strands by traversal order. 73728-check scan.
5. **Tamings consolidated** (`9d71fed`) — winding criterion foregrounded as the spine;
   ten tamings regrouped into terminal certificates vs reductions.
6. **Universal-impossibility attack branch opened** (`9d71fed`, `08c87ee`, `47b962e`).

## The paper right now

`papers/reconstruction/` — 10pp, builds clean, Type-6 bridge. Honest Type-6 with a
properly-fenced conjecture. It is NOT broken/incomplete — it's a legitimate current
state. Owed before any submission (unchanged, all USER's):
- **Verify the 6 HAND proofs.** #1 priority = hand-check the genericity-gate
  definition (it's a definitional rfl-remark, NOT a Lean target). `STATUS.md` ranks
  all 8 by verify-priority.
- **Phase-7 `/audit full`** (the winding novelty is what the second audit probes).
- Lean pruning-lemma port is TEED UP (4 design constraints in `pruning_k2_theorem.md`),
  not started — durability, not owed.

## The universal-impossibility attack — state of play

**Why it matters:** the paper's ten tamings read as "stamp collecting" precisely
because their organizing theorem (they're EXHAUSTIVE = no fork) is unproved. UI
converts "ten sufficient conditions" into "one N&S classification." The user directed:
push for it, no half measures. NAV: `notes/unsorted/universal_impossibility_taxonomy.json`
(load first) → `joint2_wielandt_finding.md` + `pruning_k2_theorem.md`.

**The reduction chain:** UI ⟸ [Joint 1: pruning lemma, CLOSED all k] ∧ [Joint 2: OPEN].

**Joint 2, where the live edge is:**
- Crux CORRECTED. It is NOT "the non-symmetric case" — golden mean is primitive +
  symmetric yet safe, and its safety is signability/TU firing on the bipartite
  residue. The real crux: **the tamings are the ONLY sources of residue-safety in a
  primitive language** (= "tamings complete").
- 3-mechanism triple (grading/potential/phase-code) COLLAPSED to 1 (phase-code =
  grading; monotone potential is constant on an SCC).
- Girth-locking attack: v1 detector was BUGGED (tested target-graph 2-colouring,
  false-negatived golden-mean signability → false near-forks). v2 fixed
  (grading = period | parity = safe-on-evens), A=3 full-enum: 0 survivors.
- **TWO DISTINCT CARRIERS of residue-safety, settled by hand:**
  - grading = the transfer DIGRAPH factors onto ℤ_d;
  - parity = the LAYERED RING is bipartite at even L ⟹ König/TU on the
    independent-set polytope.
  "A small quotient makes the relevant polytope integral" — on two different carriers.

**Counterexample track is CO-EQUAL** (UI could be false; evidence is thin —
fork-free ≤4 states / 9 edges). Deep 5-state fork-sweep (past Wielandt index, past
the artifact window) came back clean = real evidence, but keep hunting wider.

## The single best next move

**Prove or refute: on a primitive language, parity is the only source of
residue-safety** — i.e. "primitive + residue-safe ⟹ the layered ring is bipartite
(parity carrier)." LISC (raw-DFS) is the falsifier. This is settleable by THINKING on
a hand example (golden mean + one A=4 survivor), not by scanning. Outcome is binary
and both branches are wins:
- holds ⟹ the sources-of-safety crux collapses (primitive rules out grading, leaving
  only parity) → a real step toward closing Joint 2;
- breaks ⟹ the break IS the missing taming (the one that makes the catalogue feel
  complete).

**Win-condition for the whole attack (set so "no half measures" stays bounded):**
either the full theorem, OR reduction to a single named lemma that becomes the paper's
stated open problem. Both complete the paper's arc.

## Discipline notes (this session's hard-won guards — do not lose)

- **Confirmation-bias flag is ON.** Many hard things dissolved this session (k=2 gap,
  k≥3 gap, crux mis-statement, triple→1, near-forks). Some are certainly real (Joint 1).
  But a streak of "simpler than feared" from self-designed tests can precede a clean
  proof of a FALSE theorem. Keep the fork-hunt co-equal; keep raw-DFS LISC as the
  ground-truth oracle.
- **The A=4 "primitive⟹parity" data is thin AND partly circular** (`parity_explained`
  ≈ the safe-on-2ℤ filter). Recorded as a HYPOTHESIS, not a theorem. Do not round up.
- **The Lean winding kernel certifies the KERNEL given classical flow-decomposition,
  NOT the winding characterisation.** Do not recall as "winding Lean-certified."
- Every novel step is ⟦HAND⟧ until independently verified. Verify-by-building against
  the raw-DFS oracle; the v1-detector bug was caught exactly this way (provable
  inconsistency with a proved theorem, not a hunch).

## Blast-radius sync (verified this handoff)

- Git: clean tree, 7 commits.
- JSON: universal_impossibility taxonomy + registry + commensurability taxonomy all
  VALID; all detail pointers resolve.
- Paper: builds clean, 10pp, no undefined refs.
- Lean: `WindingDichotomy.lean` compiles, 0-sorry, standard axioms.
- Memory: reconstruction flagship line updated (Joint 2 crux corrected, two-carrier
  structure, new branch pointer); 22.5KB, under limit.

# spine (umbrella note) — status

Created 2026-07-08. The corpus-level umbrella reading the trilogy (Paper II /
distributivity&realism, sigma_essential, reconstruction) as one classification
of "the price of realism", organised by Paper II's filtration
S ⊇ S_σ ⊇ S_df and its three transitions (pasting / regularity / sharpness).

## Scope: skeleton, REFERENCE-ONLY (never absorbs the three papers)
- §1 observer-first moral (the corpus's stated-once thesis).
- §2 the filtration + the map table (each paper = one transition; boundary
  coordinate + separating witness per row). The filtration and its three
  transition-names are Paper II's own (verified in its abstract); the spine's
  contribution is noticing the other two papers INHABIT two of the transitions
  with constructed witnesses — NOT discovering the filtration.
- §3 what's genuinely common (non-distributivity as discriminator; the price
  list; the method; the honest novelty ledger = Type-6 bridge + 2 constructions).
- §4 the corpus-level OPEN questions, correctly hedged:
  * Conj 4.1 "one coherence notion across rungs" — stated as CONJECTURE with
    rem:onenotion flagging it is a RHYME not a proved identity (EA is pairwise-
    local; sigma-essential finite-coherence is global-f.a.-extendable; verified
    against the three papers' actual definitions — they are NOT literally the
    same notion).
  * Conj 4.2 "quarantine hinge" (provably-disjoint witnesses) — stated as
    CONJECTURE with rem:quarantine flagging C1' is ROADMAP status in the
    reconstruction seed (§2.3 "to be proved"), NOT an audited proof. The spine
    does NOT write "provably disjoint" as fact.
- §5 reading order (II→σ→recon) + dissemination order (σ first, alone).

## Verification gates checked before building (per advisor)
- C1' proof status: ROADMAP (seed §2.3 header "to be proved") — so "provably
  disjoint" is conjectural-hinge, hedged in rem:quarantine.
- EA = finite-coherence = S-rung: NOT a literal identity (partial rhyme) —
  hedged in Conj 4.1 / rem:onenotion.

## Deferred (per advisor, desync risk)
- The vocabulary-alignment pass across the three papers (unify "commensurability"
  usage; state OML conjecture identically). The spine SIDESTEPS this by
  introducing "commensurability parametrized by rung" and noting where each
  paper's local usage sits — ZERO terminology edits to the three papers. If ever
  done, sigma_essential goes LAST (ships first + standalone).
- Per-paper "position in the programme" paragraphs — additive, low-risk, not yet
  written; should reference the spine, not assert the unification.

## NOT DONE / owed
- The two bridging conjectures must be settled before the spine's central claim
  ("each paper is one transition, worlds provably disjoint") can be asserted as
  fact rather than open problem. C1' audit is the highest-value corpus lemma.

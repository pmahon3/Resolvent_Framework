{
  "name": "@prooftree/core",
  "version": "0.1.0",
  "description": "ProofStep schema types and utilities — no Lean, no rendering",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/index.test.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "ajv": "^8.12.0"
  }
}
